module Exercise4 where

isPalindrome :: (Eq a) => [a] -> Bool
isPalindrome x = reverse x

myAbs :: Integer -> Integer

myAbs - undefined

-- find the absolute value of a numebr

-- definition using fst and snd

main :: IO ()
main = do
  print (isPalindrome (radar))
